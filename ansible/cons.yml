---

- hosts: cons
  user: deploy
  sudo: True
  vars_files:
    - vars/cons.yml
  roles:
    - common
    - webserver
    - blog
    #- { role: mailserver, tags: ['mail'] }
    #- { role: znc, tags: ['irc'] }
    #- database
    #- webmail
