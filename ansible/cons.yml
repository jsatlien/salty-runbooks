---

- hosts: cons
  user: deploy
  sudo: True
  vars_files:
    - vars/cons.yml
  roles:
    - common
    - webserver
    - git
    #- { role: blog, tags: ['blog'] }
    #- database
    #- mailserver
    #- webmail
