---

default_user: cons
default_user_full_name: Brit Butler
default_user_email: brit@kingcons.io

default_user_pub_key: "{{ lookup('env', 'HOME') }}/.ssh/id_rsa.pub"
default_user_pri_key: "{{ lookup('env', 'HOME') }}/.ssh/id_rsa"

apt_mirror: http://debian.gtisc.gatech.edu
debian_release: jessie

default_build_dir: /home/{{ default_user }}/bin/builds

# TODO: Split these into vars files inside the roles?
ruby_version: 2.2.2
ocaml_version: 4.02.3
clozure_version: 1.10
